import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Coffo | About Us",
    description: "Generated by create next app",
  };

export default function AboutUsLayout({children}: {children: React.ReactNode}) {
    return (
        <section>
            {children}
        </section>
    );
}